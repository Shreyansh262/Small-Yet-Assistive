# ── Core ML / Training ──
torch>=2.1
torchvision
transformers>=4.46
peft>=0.11
trl>=0.9
accelerate
bitsandbytes

# ── Vision / Data ──
Pillow
opencv-python
numpy
datasets
huggingface_hub

# ── Evaluation Metrics ──
sacrebleu
rouge_score
pycocoevalcap
jiwer
nltk
sentence-transformers
tqdm

# ── Config / Plotting / Utils ──
PyYAML
matplotlib
requests

# ── Optional (for specific baselines) ──
# qwen-vl-utils       # only needed for Qwen2-VL baseline in evaluation/ocr_vqa/baselines
# google-generativeai # only needed if using Gemini API as LLM judge
